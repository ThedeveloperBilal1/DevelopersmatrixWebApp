// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   String?
  published Boolean  @default(false)
  authorId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Games Database Models
model Game {
  id            String   @id @default(cuid())
  name          String
  developer     String
  publisher     String
  releaseDate   String
  rating        Float    @default(0)
  price         String
  description   String
  imageUrl      String
  trending      Boolean  @default(false)
  popularity    Int      @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  genres        GameGenre[]
  platforms     GamePlatform[]
  features      GameFeature[]
  tags          GameTag[]
  minReqs       GameRequirements? @relation("MinimumRequirements")
  recReqs       GameRequirements? @relation("RecommendedRequirements")
}

model GameGenre {
  id     String @id @default(cuid())
  name   String
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
  
  @@unique([gameId, name])
}

model GamePlatform {
  id     String @id @default(cuid())
  name   String
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
  
  @@unique([gameId, name])
}

model GameFeature {
  id     String @id @default(cuid())
  name   String
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
}

model GameTag {
  id     String @id @default(cuid())
  name   String
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
  
  @@unique([gameId, name])
}

model GameRequirements {
  id          String   @id @default(cuid())
  os          String
  processor   String
  memory      String
  graphics    String
  directX     String
  storage     String
  type        String   // "minimum" or "recommended"
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  gameForMin  Game?    @relation("MinimumRequirements", fields: [gameId], references: [id])
  gameForRec  Game?    @relation("RecommendedRequirements", fields: [gameId], references: [id])
  gameId      String?
}

// User System Specs (for Can You Run It feature)
model UserSystemSpec {
  id        String   @id @default(cuid())
  cpu       String
  gpu       String
  ram       Int      // in GB
  storage   Int      // in GB
  userId    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
